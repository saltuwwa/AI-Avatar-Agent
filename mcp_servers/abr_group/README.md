# MCP: ABR Group (бонус)

Инструмент **`search_abr_restaurants(query="")`** — точки сети ABR в Алматы через тот же 2GIS API (нужен `TWO_GIS_API_KEY`).

```bash
python mcp_servers/abr_group/server.py
```
